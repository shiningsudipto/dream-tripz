import { StaticImageData } from "next/image";
export type TDestinationSlider = {
  rating: number;
  totalReview: number;
  img: StaticImageData;
  place: string;
  address: string;
  price: number;
};
